```js
  // 1. 全局安装
  import { createApp } from 'vue'
  import andExtend from 'antdy-extend'

  createApp(App).use(antExtend).mount('#app')

  // 2 . 按需导入
  import {AdInputSelect} from 'antdy-extend'

```